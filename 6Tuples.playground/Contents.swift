
let hhtpStatus = (200, "OK")

var numbers: (Int, Float, Double) = (1, 2.3, 4.5)

numbers.2

var httpStatus: (Int, String?)?

var playScores: ([Int], firstName: String, lastName: String?)

playScores = ([134_000, 128_500, 156_250], "Scott", "Gardner")

playScores.firstName

playScores.2

var (scores, firstName, lastName) = playScores

firstName = "Eric"

playScores.firstName

let (scottsScores, _, _) = playScores

var scottsScore  = (100, name: (first: "Scott", last: "Gardner"))

scottsScore.name.first = "Erric"

(10, 20, 30) == (100/10, 100/5, 100 - 70)

let ericScore = (100, name: (first: "Erric", last: "Gardner"))





		